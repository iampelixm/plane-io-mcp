# Backlog — plane-io-mcp (`mcp-plane-sync`)

Беклог **этого репозитория** (MCP-сервер Plane ↔ markdown). Для синхронизации с Plane нужны `.plane-sync.json` в корне клона и токен (см. [README.md](../../README.md)).

**Формат:** `- [ ] Заголовок` + при необходимости многострочный контекст; после синка в конце строки появляется `<!-- plane:issueId=… -->`. Секции должны совпадать с `sections` в `.plane-sync.json` (по умолчанию ниже).

## Inbox

- [ ] Несколько `backlogFiles` и архив Done: роли файлов, перенос в архив, sync без дубликатов
  Контекст: сейчас `plane_sync` читает все пути из `backlogFiles`, но `take` / `set-status` и MCP-аналоги двигают задачу только **внутри одного файла**; второй файл без доработок даёт риск дубликатов Plane issues. Подробнее — раздел *Multiple `backlogFiles` and Done archive* в [README.md](../../README.md).
  Критерии готовности:
  - В конфиге явные **роли** путей (активный беклог vs архив Done) или эквивалент без двусмысленности.
  - Переход в Done **переносит** блок в архивный файл и/или один задокументированный сценарий «close → archive».
  - `plane_sync` / `plane_sync_status` сходятся по всем файлам **без** дубликатов по `issueId` и лишних созданий issues.

- [ ] Добавить в репозиторий `LICENSE` и при необходимости обновить формулировку в README
  Контекст: в README указано «until a LICENSE file is added».

- [ ] Покрыть тестами критичные ветки `core.mjs` (например нормализация `listProjects`, разбор беклога, перемещение секций)
  Контекст: снизить регрессии при доработках sync и Plane API.

## Ready

<!-- Задачи, которые можно брать в работу без доп. уточнений -->

## Doing

<!-- Текущая работа -->

## Done

<!-- Завершённые задачи (при появлении отдельного архива — переносить туда по политике репозитория) -->
